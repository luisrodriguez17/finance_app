rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Assistant chatbot daily quotas: written only by the Cloud Function (admin).
    match /chatUsage/{uid} {
      allow read, write: if false;
    }
  }
}
